# 의료 AI 논문 다이제스트 — 2026-06-20

**생성 시각:** 2026-06-20 23:05 UTC  
**검색 범위:** 최근 48시간  
**상태:** ❌ 실패 — 네트워크 차단

---

## 오류 내용

이 환경(Claude Code on the web 원격 실행 환경)에서 PubMed E-utilities API(`eutils.ncbi.nlm.nih.gov`)가 네트워크 허용 목록(allowlist)에 없어 접근이 차단되었습니다.

```
Host not in allowlist: eutils.ncbi.nlm.nih.gov.
Add this host to your network egress settings to allow access.
```

---

## 해결 방법

Claude Code on the web 환경 설정에서 아래 도메인들을 **네트워크 이그레스(egress) 허용 목록**에 추가해야 합니다:

| 도메인 | 용도 |
|--------|------|
| `eutils.ncbi.nlm.nih.gov` | PubMed E-utilities API (필수) |
| `pubmed.ncbi.nlm.nih.gov` | PubMed 웹 인터페이스 (선택) |

설정 방법: [Claude Code on the web 문서](https://code.claude.com/docs/en/claude-code-on-the-web) 참고

---

## 다음 실행

네트워크 설정 수정 후 다시 실행:

```bash
python medical_ai_papers.py --save --push --hours 48 --max 7
```
